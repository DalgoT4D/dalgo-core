# Tasks — Resource Sharing (Layer 1: Content)

**Plan:** [plan.md](./plan.md) · **Research:** [research.md](./research.md)
**Branch:** `feature/resource-sharing` (fresh off **main** — the rbac PRs merged: DDP_backend #1414 → `d02cc41b`, webapp_v2 #331 → `11a2b523`, so the plan's "stacked on feature/rbac" strategy is obsolete) · **Worktrees:** `../.dalgo-worktrees/resource-sharing/{DDP_backend,webapp_v2}` (recreated 2026-07-09 from origin/main; the old stacked worktrees' branches archived as `archive/resource-sharing-rbac-stack-20260707`)
**Execution mode:** subagent-driven — fresh implementer per task + task review per task + final whole-branch review. **No pushes to GitHub.**
**Restarted:** 2026-07-09 (baseline facts re-verified on main: migrations end at 0167, `core/sharing` greenfield, `/api/access` + `/api/groups` prefixes free, seed has only `can_share_dashboards`)
**Ledger convention:** when a task's review comes back clean, its checkbox flips to `[x]` with `(commits <base7>..<head7>)` appended — this file is the recovery map.

**Slicing note:** `ResourceShare` model moved from M2 into M1-T2 — the resolver's `principal_match_q`/`accessible_filter` contract needs the grants table to be testable. M2 keeps the endpoints + UI.

**Setup note:** a broken pre-plan attempt (old "floor" naming, missing `models/sharing.py`) was found uncommitted in the backend worktree — stashed as `aborted pre-SDD resource-sharing attempt`.

## Milestone 1 — General access + owner + resolver (backend)
- [x] T1: Models + migrations 0168+ — `general_audience`/`general_level`/`owner` on Dashboard/ReportSnapshot/Metric/KPI/Alert; `owner` on Chart; `created_by` CASCADE→SET_NULL ×5; OrgPreferences +3 fields; backfills; `can_delete_resource()` owner-first (commits d53ccf4..4c30685, review Approved; minors for final review: dead `general_*__isnull` filter in 0169 backfill, no OrgPreferences-field tests, `help_text` only on ReportSnapshot owner)
- [x] T2: `ResourceShare` model + `RESOURCE_TYPES` table (`core/sharing/shareable_types.py`) + resolver (`core/sharing/access_resolver.py`: `effective_permission`, `principal_match_q`, `accessible_filter`) — pure-function truth-table tests; writes go in `core/sharing/sharing_actions.py` (one file, per plan §4.0) (commits 4c30685..9065fe5, review Approved; minors for final review: no member-cap-via-grant test cell, `created_by`-only filter branch untested in isolation, `Cast→BigInteger` in accessible_filter needs a Layer-2 warning comment; T12 must grant old owner explicit Edit on transfer)
- [x] T3: Wire `accessible_filter` into the 5 list services (dashboards/reports/alerts/metrics/kpis); query-count tests; charts list unchanged (commits 9065fe5..5beb64b, review Approved; 403 chosen for in-org-not-shared, matches existing pattern; minors for final review: extra PK fetch in `get_snapshot_view`, resolver's unconditional `created_by` list-filter vs owner-first detail could diverge post-transfer [same root as T12 note]; grants-on-metric/kpi admit viewers by design — enforcement is write-side in T5's POST-grants 400)
- [x] T3b: **Follow-up from T3 review** — view-gate the content sub-endpoints that leaked private-resource data by id (brief's table + sweep: `share_report_via_email`, report comment GETs, `duplicate_dashboard`, `list_dashboard_datetime_columns`, `get_filter`; helper in `core/sharing/gates.py`) (commits 5beb64b..5ae0783, review Approved; minors for final review: extra PK fetch per gated endpoint [same trade-off as T3], comment GETs keep pre-existing 400-not-404 convention)
- [x] T3c: **Follow-up from T3b review** — `get_kpi_summary` scoped with `accessible_filter` like `list_kpis` (commits 5ae0783..a9a9633, review Approved, zero findings)
- [x] T4: Chart data access context — `?dashboard_id=` on chart data/detail; Member standalone → 403; `run_chart_query` seam (`core/sharing/chart_access.py`, executor injected so core never imports API) (commits a9a9633..335b3f7, review Approved; **lockstep dependency: T6 frontend MUST send `dashboard_id` from dashboard tiles or Member dashboard viewing breaks** — added to T6 brief; minors for final review: anti-oracle rests on Members lacking dashboard-edit slugs — add guard comment/test; pre-existing reachable 500 at charts_api.py:1030 `chart.created_by.user.email` on SET_NULL'd creator — one-line guard; `column_exists` metadata query bypasses `run_chart_query` seam [post-gate, note for Layer 2])

## Milestone 2 — Grants + share modal core + badges
- [x] T5: `/api/access/{rtype}/{id}` GET + `/grants` POST/DELETE + `/general` PUT (narrow warn-and-offer); seed migration 0171 (slugs 86-89 → super-admin/admin/analyst, Redis-key delete, idempotent); resolver edit-guards on the 5 update endpoints; dynamic registry-slug gate mirrors `@has_permission` (sanctioned — generic routes) (commits 335b3f7..db5b4d7, review Approved, all escalation attacks defended; minors for final review: GET overview exposes all grantee emails to any view-holder [sanctioned, product-awareness], `/api/access/*` routes carry no static decorator [sanctioned]; note: model default is `all_users/view`, not private)
- [x] T5b: **Follow-up from T5 review** — edit-gated dashboard filters + locks and kpi notes writes via `require_edit_access`; sweep verified complete; sharing toggles stay creator-only (strict subset of edit) (commits db5b4d7..2375851, review Approved; minor for final review: lock holder whose edit access is revoked mid-session can't refresh/release own lock — mitigated by lock expiry)
- [x] T6: Frontend — `useResourceAccess` hook + ShareModal people/General-access sections (capability-flag-driven, narrowing confirm flow) + `PERMISSIONS` const + dashboard tiles send `?dashboard_id=` (lockstep verified); badges NOT built (list DTO lacks fields — sanctioned deferral to T6b/T6c); dead "Show only shared" filter removed (predicate keyed off never-serialized `is_public`) (webapp commits 11a2b52..9cfce7e, review Approved; minors for final review: add-person Combobox missing data-testid, narrowing confirm panel snaps Select back to old value [UX polish])
- [x] T6b: **Backend follow-up from T6** — (a) dashboard DTO gains `general_audience`/`general_level`/`is_owner`/`is_creator`; (b) `orguser_id` in org-users + currentuserv2 responses; (c) `chart-data-preview` (+ `total-rows` twin) + `map-data-overlay` gated: `chart_id`+`dashboard_id` params → `require_chart_view_access` + schema/table match guard; config-only paths re-gated Analyst+ (closes pre-existing Member arbitrary-table hole via no-op `has_schema_access`) (commits 6e8d03d..64dcfba, review Approved; **Important → T6d: column-level residual — context-admitted Member can read other columns of a chart-visible table via crafted dimensions/metrics/filters; net-new exposure on map-data-overlay**; minors for final review: `is_owner` is 4th copy of owner-fallback comparison, dead `has_schema_access` call after new gate)
- [x] T6c: **Frontend follow-up from T6/T8** — badges + "Shared with you" + working shared filter; Add-person + Add-member enabled via `orguser_id`; table/map tiles send context params (query vs body verified per endpoint); `AccessGrant.email` → `string | null` (webapp commits 4458f93..0b337fe + fix ca6cfae, review Approved after 1 fix round [filter-count omission + general_level tooltip, both re-verified red/green]; minors for final review: GENERAL_LEVEL_LABELS duplicates ShareModal's private LEVEL_LABELS, native title tooltip invisible on touch devices; accepted carry-overs: "Shared with you" false-positive in frontend-before-backend deploy window [rollout is backend-first], product question: badge may be noisy for general-access-visible dashboards)
- [x] T6d: **Backend follow-up from T6b review (FP1)** — `require_payload_within_chart_config` guard on the dashboard-context branch of the 3 chart POSTs; hidden-column reads all blocked (reviewer attacked empirically); Analyst+ standalone byte-identical (commits 64dcfba..cf7856b, review Approved; deliberate deviation accepted: filters may use displayed columns [drill-down]; **deferred to Layer-2 `run_chart_query` seam: context callers can drop/alter saved WHERE clauses — row-set widening within displayed columns; threat model updated in plan §5**; follow-ups for final review: org-scope `Metric.objects.get` in `_resolve_saved_metrics` + off-context `DashboardFilter id__in`, no standalone pin for total-rows, `column_expression` raw SQL free for Analyst+ standalone [pre-existing], `has_schema_access` still a stub)

## Milestone 3 — Groups
- [x] T7: `UserGroup`/`UserGroupMember` (`ddpui/models/user_group.py`, migrations 0172-0173) + `/api/groups` CRUD + group grants live end-to-end (resolver `get_group_ids` seam filled as default — `principal_match_q` internals verified behavior-preserving, 33 truth-table tests unmodified green; new slugs `can_manage_user_groups`/`can_view_user_groups` → admin+analyst) (commits 2375851..6187c0b, review Approved; minors for final review: `auto_created=True` cosmetic kwarg on created_at, `add_member` get_or_create won't re-activate a pending member [matters at M4]; product-confirm: member add/remove gated creator-or-admin)
- [x] T8: Frontend — `useUserGroups` hook + `app/settings/groups` page (counts, collision-inline, creator-or-admin gating UX-only w/ backend authoritative) + ShareModal group picker/rows fully functional (member-add + person-add stay disabled pending T6b's orguser_id) (webapp commits 9cfce7e..4458f93, review Approved; **Important → fix in T6c: `AccessGrant.email` typed non-nullable but backend sends null for group grants — flip to `string | null` + fix test mocks**; minors for final review: GroupDetailDrawer fetches org users unconditionally on mount, inert "(invite pending)" badge ahead of M4, CAN_VIEW_USER_GROUPS slug unused in app code)

## Milestone 4 — Invites + pending + expiry
- [x] T9: `Invitation.expires_at` (migration 0174, backfill) + share-flow email grants (in-org → instant active; unknown → Member Invitation + pending ResourceShare) + activation on accept (grants + group memberships) + resend refresh + daily celery cleanup + share-path hardcodes Member (role field deferred — additive later, reviewer-adjudicated) (commits 6187c0b..6e8d03d, review Approved; **stated security property, inherited from invite_user_v1: share-by-email with a cross-org platform user's email instant-adds them as Member [same as today's invite flow] and the active-vs-pending response reveals account existence — product should revisit**; minors for final review: 4 unpinned attack-path tests [re-invite dedupe, email case e2e, over-cap-no-invitation, cross-org-email instant-add], `invited_email` max_length=50 pre-existing, side-effect fix of resend-branch missing .save())
- [x] T10: Frontend — multi-email paste (chips, mail-client `Name <email>` unwrapping, invalid-token rejection, case-insensitive dedupe), batched POSTs w/ per-email failure retention, pending rows get working permission dropdown (email-path re-POST) + remove; plan deviation sanctioned: "join as Members" hint instead of role picker (webapp commits ca6cfae..e473b44 [05ec3e2 feature + eb770fa layout-revert + e473b44 fix round], review Approved after 1 fix round [EMAIL_REGEX angle-bracket hole + dead pending dropdown]; minors for final review: space-only-separated multi-address paste honestly rejected [could unwrap], pending permission change re-sends invite email [backend polish: permission-only update path in upsert_grant], PeopleWithAccessSection 331 lines vs 300 warn threshold, no hard cap on pasted emails)

## Milestone 5 — Public links global + kill switch
- [x] T11 (backend): kill switch enforced — both toggles (enable-only; unpublish always allowed) + all 16 token-gated public renders (404, non-destructive, revival pinned; org derived from resource; X-Render-Secret PDF path deliberately exempt, pinned); `PUT /api/orgpreferences/sharing/` admin-gated for the 3 fields; org-default General access seeded at 6 create paths incl. `duplicate_dashboard` (org default, NOT source copy — security-conservative) via one helper (commits cf7856b..56210ff, review Approved; minors for final review: role-check-vs-slug gating split [disclosed], dead-link hits not counted while switch off [correct])
- [x] T11f (frontend): `/settings/access-management` (RoleGuard ADMIN_ROLES, same pattern as billing/user-management) — kill-switch toggle + org-default audience/level pickers → `PUT /api/orgpreferences/sharing/` (helper text correctly says new-resources-only); ShareModal public-link section hidden only on explicit `allow_public_sharing===false` (no-row/loading stays visible, mirrors backend default) for dashboard AND report; reports unified onto ShareModal, `report-share-menu.tsx` + both bespoke dialogs deleted; report share gate corrected `can_share_dashboards`→`can_share_reports` (reviewer-verified vs seed 0171, role parity holds); one order-dependent fake test removed (reviewer reproduced mock-bleed on base — `clearAllMocks` doesn't reset `mockImplementation`) (commits fe55d4a4..de6d98fb w/ T12f, review Approved, 9 suites/92 tests reviewer-run; **product call for final review: report email SUBJECT field dropped with retired dialog [only loss — personal-message field is a gain]**; minors for final review: transfer picker/confirm shows email not name, `useUsers()` fires on every modal open even when transfer hidden [SWR-cached], confirm panel hardcodes orange-* tokens [not dark-mode-aware])

## Milestone 6 — Ownership transfer
- [x] T12 (backend): `POST /api/access/{rtype}/{id}/owner/` — owner/admin-only (`require_owner_access` via `can_delete_resource`), slug-gated, same-org active new owner, owner flip + old-owner Edit grant atomic in one txn, upsert (round-trip idempotent, reviewer ad-hoc verified), works on all 5 rtypes incl. grants=False (flag gates endpoint, not action — plan-consistent) (commits 56210ff..6bb6829, review Approved; minors for final review: no DB unique constraint behind grant upsert [pre-existing systemic], round-trip + slug-gate-on-transfer tests verified ad hoc but not codified)
- [x] T12f (frontend): owner row + transfer in ShareModal — visibility from server `is_owner` || ADMIN_ROLES; picker keyed on `orguser_id` (OrgUser PK, pinned by hook test); confirm copy truthful re old-owner-keeps-Edit (matches `transfer_ownership` unconditional grant); errors surface backend messages via `toastError.api`; rtype-agnostic incl. grantless metric/kpi (commits w/ T11f above, review Approved; ⚠️s resolved by coordinator: `can_view_orgusers` held by ALL roles incl. member → non-admin owner CAN populate picker [verified seed 002/003]; metric/kpi have no ShareModal caller BY PLAN [§ scope table: "no share modal"]; **alerts had NO ShareModal caller — genuine cross-task gap → folded into T17f scope item 8**)

## Milestone 7 — Alerts
- [x] T13: `{"type":"group"}` recipient entries — same-org validated at create/update AND re-filtered at fire time (defense in depth); expansion → active members, dedupe on orguser_id, deleted/empty group graceful-skip w/ logging; notification footer carries alert id + deep link; recipients grant no resource access (pinned) (commits 6bb6829..abcffb3, review Approved; minors for final review: skip logs at info not warning, external-vs-member email non-dedupe [duplicate email, not a leak], `/alerts?alertId=` deep-link shape is a guess pending frontend alignment in T16)

## Milestone 8 — Comments re-gate
- [x] T14 (backend): comment create relaxed to `can_view_dashboards` + resolver-view (Member on viewable report can comment; private → 403 pinned); moderation (update/delete others') → author-or-resolver-edit via `_can_moderate`; author self-rights + T3b GET gates + 400-convention untouched; anonymous pinned via real HTTP + zero public comment routes (commits abcffb3..14a60d8, review Approved, zero findings; note: create's object gate is API-layer-only, matching existing `update_snapshot` convention; `CommentPopover` capability prop → frontend batch T16)

## Milestone 9 — Request-access
- [x] T15: `AccessRequest` (migration 0175, requester CASCADE deliberate) + create/list/approve/decline in new `core/sharing/access_requests.py`; approve inserts grant at decided permission (downgrade-only, upgrade blocked); decide gate = owner/admin WITHOUT share slug (endorsed — Member owner can decide, per-requester grants only); expiry + cleanup extension; notifications w/ admin fallback (commits 14a60d8..73df5bb, review Approved; minors for final review: decision path lacks DB-level concurrency guard [owner-only race, duplicate notification worst case — suggested `.filter(status="pending").update()` hardening], null-owner notification fallback untested)
- [x] T16: Frontend — 403 intercept via `lib/api.ts` `.status` stamp + `getApiErrorStatus` (no other consumer reads it, seam verified vs backend 403s); RequestAccessScreen (POST shape/maxLength match schema, pending-on-revisit, 400→reload); ShareModal in-modal decisions from server-filtered `incoming` w/ downgrade-capped picker (backend rank-400 unreachable); CommentPopover `canModerate`=resolver-edit (matches `_can_moderate`) + sanctioned summary-popover bugfix w/ inline rationale; alerts `?alertId=` deep-link + 403 screen; metric/kpi descope confirmed correct (list-render, no per-id fetch → no 403 to intercept) (commits 07cdb036..fe55d4a4, review Approved, 84/84 new-suite tests reviewer-run; **cross-task finding → T15b, done below**; minors for final review: already-have-access recovery matches message text only [combine w/ status===400], pending-request note `truncate`+title unreadable on touch [line-clamp], alerts highlight only on loaded page [pre-existing pattern])
- [x] T15b (backend follow-up from T16 review): metric/kpi notification deep-links fixed in `_DEEP_LINK_PATH` (`/metrics?highlight={id}`, `/kpis?open={id}`) — one map feeds both request-created and request-decided notifications; other 3 rtypes byte-identical; reviewer cross-checked frontend `searchParams.get()` reads and grepped for stale `metricId`/`kpiId` param reads (none); end-to-end test pins literal URL in persisted Notification.message (commits ef7b07f..d442700d, review Approved, zero findings; full suite 2565/2)

## Milestone 10 — Bulk
- [x] T17 (backend): `POST /api/access/bulk/` — per-item slug+resolver-edit gates (verified genuinely per-item), apply-where-possible w/ `{applied, skipped[{rtype,id,reason}]}`, email path invites ONCE for N pending grants, aggregated narrow confirm (immediate-apply split real; foreign remove_grant_ids → 404 whole request), toggle_public w/ kill-switch rules via new generic `set_public`, cap 100, cross-org indistinguishable `not_found`, per-item atomicity pinned (commits 73df5bb..bec17c9, review Approved; **carry-forward → T11b: legacy single toggles are creator-only while bulk uses slug+edit — migrate them onto `set_public`**; minors for final review: email-path row loop lacks per-item try/except [practically unreachable], skipped list groups gate-skips before action-skips, `validation_error` has no detail field [additive later])
- [x] T11b: **Backend follow-up from T17 review** — both single toggles migrated to slug + resolver-edit via `set_public` (kill-switch in one place, responses byte-compatible); fixed pre-existing `toggle_report_sharing` decorator slug (`can_share_dashboards`→`can_share_reports`, role-parity verified) (commits bec17c9..3c62ccf, review Approved; note: `share_report_via_email` still on `can_share_dashboards` — out of scope, for final review)
- [x] T11c: **Follow-up from T11b review** — both sharing-status GETs relaxed to `require_view_access` (report's via the same layering pull-out as 11b's toggle); no token leak when is_public=false (schema drops it, reviewer-verified); toggles stay edit-gated (commits 3c62ccf..ef7b07f, review Approved, no findings; dashboard status GET had zero prior coverage — new test class added)
- [x] T17f (frontend): multi-select bulk bars on Dashboards/Reports/Alerts (`useMultiSelect` hook + `bulk-share-dialog.tsx`, Charts pattern mirrored — extraction declined, Charts selection partially dead code); plain-language map for all 9 skip codes + graceful unknown fallback; narrow-confirm re-send verified correct (`remove_grant_ids` key present on re-send, no confirm loop); 100-cap disables checkboxes (raw 400 unreachable); no client-side permission pre-filter; **+ per-item Share on Alerts list (gap closure) — ShareModal `entityType="alert"`, no modal conditionals**; fix round 1 (bea8228b): bar shows true cross-page count + "N on other pages", dialog snapshots items at open w/ empty guards (fixed 2 Important), header-uncheck deselects page only (commits de6d98fb..bea8228b, review Approved after 1 fix round, 6 suites/57 tests reviewer-run; minors for final review: confirm-panel "people" wording covers groups, partial result + pending confirm render simultaneously, loading-skeleton lacks checkbox column, documented exhaustive-deps disable on snapshot effect; ⚠️ not-testable-in-jsdom: visual badge refresh e2e, live add_grant sans email key [schema Optional, mirrors single-item])

## Milestone 11 — Verify
- [ ] T18: backfill verification; full suites; final whole-branch review; **ask user** before Playwright browser pass
  - [x] Backend verification PASS_WITH_NOTES: backfills 100% (all owner-NULL-with-creator rows post-date migration 0169 — verified via created_at vs migration timestamp); NO legacy email-share rows exist (searched models+migrations; `share_report_via_email` persists nothing); suite 2565/2 at d442700d; `makemigrations --check` clean. Note for PR: creation code never sets `owner` on insert (harmless — resolver/ownership/transfer all fall back to created_by; flagged for team to confirm intent)
  - [x] USER DECISIONS (2026-07-14): I2 `last_modified_by` CASCADE → **PR-note only** (fast-follow ticket, no migration now); report email subject field → **stays dropped** (backend default subject); Playwright three-role browser pass → **RUN IT**
  - [x] Final webapp whole-branch review (22 commits 11a2b523..bea8228b): **READY WITH NOTES**; suite 151/1672/4 zero fail, lint clean; invariants held (api-calls confinement, ShareModal zero entityType conditionals, PERMISSIONS complete, no `any` in 4589 lines, no PII in 22 events, `.status` stamp additive w/ exactly 3 consumers, report-share-menu deleted zero orphans, app/layout.tsx net-zero); Important I1-fe = backend T11b carry-forward (legacy toggle creator-only, fail-closed) ship-with-note; fix-now minors M1+M3 FIXED (14027e62 + 0ca351e5 env.local-untrack cleanup): `lib/access-labels.ts` single vocabulary source consumed by AccessManagement/ShareModal/BulkShareDialog/badges; already-have-access reload requires status===400 AND message match; suite 1673/4; residual note: request-access-screen has its own LEVEL_LABELS ("View"/"Edit") outside M1's 4 named surfaces → follow-up. **FINAL WEBAPP VERDICT: READY WITH NOTES at 0ca351e5**; 8 jsdom-unverifiable items → Playwright list (final-review-webapp.md)
  - [x] Final backend whole-branch review (22 commits d53ccf49..d442700d): verdict NOT READY pending I1 → then READY WITH NOTES. Resolver verified pure/unbranched, 33 truth-table tests zero-diff since T2; default-deny at every endpoint; kill switch on all 16 public routes, X-Render-Secret properly hmac-gated; migrations reversible+idempotent, Redis key-delete pattern correct; no IDOR/escalation paths. I1 must-fix FIXED (07a15637): all 10 sites guarded w/ Dashboard/Report idiom + schemas Optional + 6 red→green null-creator tests, reviewer re-swept zero remaining, suite 2571/2; **FINAL BACKEND VERDICT: READY WITH NOTES at 07a15637**. PR-description notes: I2 `last_modified_by` still CASCADE on Chart/Dashboard (user decision: fast-follow migration 0176 vs disclose-only), I3 pre-existing unscoped Metric/DashboardFilter fetches → fast-follow ticket, dead `chart_schema.py` (singular) cleanup, owner-not-set-at-creation (fallback-safe), all ~40 minors ship-with-note + 11 residual-risks-by-design (tables in final-review-backend.md)

## Design alignment (post-v1) — from design-alignment-plan.md (2026-07-15)
- [x] Phase A: quick wins — COMPLETE (backend 07a15637..02376b8a, webapp 0ca351e5..5b245cb1; review Spec ✅ Quality Approved; minors for final review: TZ-dependent date test, raw entityType in transfer copy for metric/kpi, unbounded member prefetch at scale; ⚠️ requests-count resolved: backend upserts one pending per requester+resource)
- [x] Phase D: sharing emails — COMPLETE, merged ff into feature/resource-sharing (02376b8a..b7b71e77; review Spec ✅ Quality Approved; all 5 grant-creation paths traced clean, no txn hazard; minors: server-UTC date, 3rd frontend-url fallback copy, on_commit belt-and-braces note)
- [x] Phase B: settings consolidation — COMPLETE, merged ff into feature/resource-sharing (webapp 5b245cb1..8e4c1eac; review Spec ✅ Quality Approved; gating = true conditional render, no analyst bypass; minors for final review: tab state not re-synced on soft nav, legacy role-name tooltip vs new Roles copy one tab apart, browser smoke pass still owed)
- [x] Phase C: share-modal rework — COMPLETE after 1 fix round, merged into feature/resource-sharing (webapp 5b245cb1..fd2c5a2c merged at 82a8789c; backend 02376b8a..e839efae merged at b62dd982; review Spec ✅ Quality Approved; invite_role gate layer-verified, stricter than legacy invite check; fix round closed browse-on-focus + double-submit + residual-text; minors for final review: bulk invite_role 403 untested on bulk endpoint, Set<string|undefined> typing wart, unvirtualized browse list, non-@ residual blocks SHARE [reviewer: acceptable, one-line polish available])
- Decisions pending: D1 roles-matrix vs audience×level; D2 transfer consent; D3 designer adds General-access to modal frames; D4 archive obsolete chart-extend warning frame; D5 bulk share fires N shared-with-you emails per selection (bulk-invite sends 1) — suppress/digest/keep?

### Final round (2026-07-15)
- [x] Final cross-phase review: READY WITH NOTES — all 4 seams clean (B×C links/testids, C×D retry no duplicate emails, invite path no double-email incl. bulk, A columns survived B move); 4 fix-nows landed (webapp 72d6ac8a: TZ-safe date test, RESOURCE_NOUNS transfer copy, Roles-vocabulary tooltip; backend faa98f60: bulk invite_role escalation 403 test)
- [x] Sandbox schema drift diagnosed + fixed: dev DB is a multi-branch migration hybrid; only real drift vs branch models was dashboard.owner (old attempt's db_column) vs owner_id — renamed IN SANDBOX ONLY. Dev DB untouched (other branches expect `owner`). NOTE for branch deployment: staging/prod migrate 0168 fresh = fine; the shared dev DB will need the same one-column rename when the branch runs there.
- [x] Browser pass (sandbox 3002/8003, true branch backend): login clean, Access page (3 tabs, Created By, avatar stacks, Created), old-route redirect w/ ?tab=people, share modal browse-on-focus (groups first, dup-guards), staging, SHARE commit → grant live. FOUND: browse dropdown renders in-flow, stretches modal past viewport (SHARE unreachable at 1280x720) — fixer dispatched; minor: Escape closes whole modal not just dropdown.
- Known test-infra note: rotating jest flake under full parallel runs (AlertWizardModal + timing-sensitive share-modal suites) — pass in isolation; follow-up: fake timers or longer waitFor.

### RBAC screens gap review (2026-07-15)
- [x] 73 exported design screens (`RBAC screens/`) compared against branch tips (webapp 812513ab, backend faa98f60) by 9 parallel topic agents — per-topic reports in `screens-review/`, synthesis + batched plan in `screens-gap-plan.md`
- [x] D1 CONFIRMED from pixels: design's Roles tab is per-role dropdowns (Analysts/Members each No access·View·Edit) + Data & Pipeline column — not expressible in built audience×level pair
- [ ] Batch 1 polish (P1–P9: owner-row placement, CANCEL label, invite copy, icons, bulk bar layout)
- [ ] Batch 2 features (F1–F6: empty states, remove-access confirm modal, HTML invite email, group create-with-members, member role badges + sorting, inline notification approve/deny)
- [ ] Batch 3 blocked on decisions D1/D2/D5/D6–D11 (see screens-gap-plan.md §3)
- [x] Batch 1 polish (P1–P9) complete: commits 812513ab..7ef49333 (f95afd1f impl + 7ef49333 P6 badge fix); review spec ✅ / quality Approved after one fix loop (P6 link-glyph badge); full jest 155 suites green; accepted notes: owner row has no avatar/role tag (needs backend field), P2 locked sentence applies to analysts too (backend-aligned; design mock self-contradictory), P8 scoped to dashboards list only (reports/alerts bars untouched — follow-up)

### Batch 2 (2026-07-16)
- [x] F2 remove-access confirm modal: rs-b2-modal a2cf62b9 (base 7ef49333), review Spec ✅ / Approved, 8 suites 101 tests green — awaiting merge with rest of batch
- [x] F3 branded HTML invite email: rs-b2-email 795508dc (base faa98f60), review Spec ✅ / Approved (design copy verbatim; deviation to email_templates.py HTML pattern adjudicated correct — Phase D emails are plain-text); 186 tests green; Minor follow-up: same-org re-invite branch untested
- [x] F6 inline notification Approve/Deny: rs-b2-notif-be c5e0b1f9 + rs-b2-notif-web 4e512e2a, review Spec ✅ / Approved (payload survives wire, design match verified); 68+130 tests green; Minor notes: stale metadata renders live buttons until clicked (degrades correctly), owner-gating implicit via recipient resolution (server re-enforces)
- [x] F1+F4+F5 Access-page cluster: rs-b2-access 470ded1f + rs-b2-access-be 8fa9d9f2, review Spec ✅ / Approved; 37 pytest + 72 jest green; FLAG (non-blocking): "Learn how roles and access work" docs link guesses /access slug — confirm with docs team; Minor: formatRoleLabel duplication, member-picker rows lack role tags vs mock
- [x] Batch 2 MERGED: webapp 4294e519 (fb949e7c F2 + b1eacff6 cluster + 4294e519 F6), backend 55486aa0 (a7e2f2cf F5 + 601dd4b3 F3 + 55486aa0 F6) — zero conflicts; merged trees green: full jest 156 suites / 1743 passed, merged-area pytest 509 passed; migration 0176 applied to sandbox
- [x] Batch 2 browser pass (sandbox, merged code): F2 remove-access dialog (cancel keeps / Delete removes — verified live on a real grant); Groups sorting chevrons + role badges (Admin/Member) in drawer; F4 create-group-with-members one-step (created + cleaned up test group); F6 notifications row shows DENY/APPROVE inline, approve resolves in place to "Approved" and grant lands server-side (effective_permission=view, verified via shell). Note: deep link in notification says localhost:3001 (backend FRONTEND_URL env default — sandbox config, not code)

### D1 permission-model rework + create-group typeahead (2026-07-16)
- [x] B2b create-group unified typeahead IMPLEMENTED directly on feature/resource-sharing: backend c567432c+7bc10df6 (add-member-by-email reusing share-flow invite path, admin-gated invite_role), webapp 9ebfb445+ecd37f60 (extracted share-modal staging machinery, groups flatten-to-active-members at submit); full suites green both repos — review in progress
- [x] D1 frontend: rs-d1-webapp 0c36a6a6 (ca440206 table+modal, 306191ea stale-draft fix, 0c36a6a6 consumer fixes) — review Spec ✅ / Approved after one fix loop (reviewer traced paired backend schema and caught: BulkShareDialog would 422, dashboard badge would vanish; all fixed + badge now derives from per-role pair)
- [ ] D1 backend: rs-d1-backend implementer running (analyst_level/member_level both layers, migration 0177, resolution rewrite)
- Follow-ups: dead AccessAudience/AUDIENCE_ORDER exports cleanup; designer asks — D3 (+modal two-row + badge-collapse semantics), permission-empty-state segmented-control vs dropdown question
- [x] B2b group typeahead review: Spec ✅ / Approved (all 7 share-modal suites green post-extraction; invite-role course-correction validated; 403-before-side-effects proven; activation via existing path proven) — fixer dispatched for Important non-blocker: flatten-fetch failure silently swallowed in GroupFormDialog resolveMembers (+ stale test comment)
- [x] ULTRACODE audit workflow launched (wf_6450de5b-906): 6 dimension finders (security-rbac, cross-repo-seams, migration-data, design-fidelity, test-quality, state-races) over today's 4 commit ranges → dedup → 2 adversarial refuters per finding
- [x] B2b flatten-fetch fix landed: webapp 24bda47f (+91f589fd stray-file cleanup), backend b379e378 (comment) — tips now webapp 91f589fd / backend b379e378
- [x] D1 backend: rs-d1-backend f93c70df — review Spec ✅ / Needs changes (1 Important: missing-prefs fallback none/none → restore view,view; fixer dispatched). Reviewer pre-verified: merge seam vs group branch CLEAN (merge-tree), frontend handles DashboardResponse/public_api renames. RELEASE NOTE: all_users/edit resources migrate to member_level=edit (members gain real edit where old ladder capped at view — spec-pinned)
- [x] ULTRACODE audit complete (42 agents, 2.36M tokens): 18 raw → 9 confirmed / 9 refuted (refuters correctly recognized already-fixed items). Confirmed: 2×Important badge-misrepresents-per-role-access (dashboard-list-utils), 1×Important general-access dropdown race (share-modal), 1×Important cross-org instant-provisioning untested in group consumer (pin + product decision D12), 4×Minor group-dialog polish, 1×Minor same-org re-invite test gap. Two fixers dispatched (rs-d1-webapp: badge+race; main branch: polish+pinning tests)
- [ ] NEW PRODUCT DECISION D12: pasting a cross-org platform user's email (share flow AND group add) instantly provisions them into the org as an active member — inherited T9 behavior, now pinned by test; needs product review (consent? invitation instead?)
- [x] D1 backend APPROVED: rs-d1-backend bf9578df (f93c70df + fallback fix w/ migration 0178 view/view org-default) — re-review Spec ✅ / Approved; 550 tests green independent run
- [x] D1 frontend APPROVED: rs-d1-webapp d17d7e08 (badge honest across all 9 combos w/ "Custom access" + per-role tooltip; general-access race guard covers PATCH-in-flight AND confirmation-pending with no re-enable gap) — re-review Spec ✅ / Approved; designer-ask additions: "Custom access" wording, narrowing panel lacks dismiss affordance (pre-existing)
- [x] Audit polish + pinning tests landed: webapp 6f7555de (+e47220be), backend 4111b849 — cross-org instant-provision + same-org re-invite behaviors pinned by tests (D12 open)
- [x] D1 MERGED into feature/resource-sharing: webapp 0e242336, backend cb30b700 — zero conflicts; merged trees green (jest 156 suites / 1763 passed; pytest merged-area 509 passed); sandbox migrated 0177+0178, backend restarted
- [x] Browser verification of user-flagged screens: Roles tab MATCHES design (Default-permissions table, locked Admin chip, per-role dropdowns, static Data & Pipeline chips, SAVE/CANCEL; save persisted analyst=edit/member=view — previously unstorable); create-group dialog MATCHES design (Add group name placeholder, unified typeahead "Add people, groups, or paste emails" / "Type or paste emails…", browse dropdown with role tags + Group tags interleaved alphabetically)
- Worktrees rs-b2-* and rs-d1 retained for now (branches merged) — can be pruned with git worktree remove when convenient

### DALGO-1532 test-plan audit (2026-07-16)
- [x] All 60 checklist items verified read-only by 6 agents against webapp 0e242336 / backend cb30b700: 44 PASS(tested), 11 PASS(built-untested), 5 OBSOLETE(Q0), 0 FAIL — full evidence in testplan-audit/ (SUMMARY.md + per-section tables)

### v1.1 chart sharing (2026-07-16)
- [x] Decision: charts independently shareable (Q0 #1 reversed; Q0 #2-4 stand). Confirmed: no chart public links; member chart grants DEFERRED (member_level pinned none); existing charts migrate analyst=edit/member=none
- [x] Research (v1.1/research.md): 2 security finds — public dashboard links leak EVERY org chart (public_api.py:191,256 no tile check) → M0 fix; list_charts unscoped. Reports immune (frozen configs). Embed hook = ChartSelectorModal + server validation (update_dashboard is blind today)
- [x] Plan written: v1.1/plan.md (M0 leak fix → M1 model/registry → M2 coverage+warnings → M3 frontend → M4 requests)
- [ ] Execute v1.1 plan (awaiting go)

### Design-alignment round 3 (2026-07-16, user frame reviews)
- [x] Share modal "New users" frame fidelity: f3c08d61 on feature/resource-sharing — review Spec ✅ / Approved (title Share "{name}", amber callout+icon, staged amber icon, labeled invite-role dropdown); analyst-mock inconsistency adjudicated (locked sentence kept)
- [ ] Share-modal FULL FLOW alignment (9 frames): rs-shareflow implementing (collapsible requests header w/ Deny/Approve, search label, role tags on access rows, plain-text Owner, borderless View⌄ controls). NOTE: "Extend all 7 charts" broadening dialog frame = v1.1 chart plan (awaiting go), excluded
- [ ] People merged list: rs-people 04c04d1f done (envelope/person icons, no sub-tabs) + follow-up running: org-wide invitations + invited_by (backend rs-people-be) replacing viewer-scoped derivation
- [ ] Group modal chips-in-input (teal internal/amber external) + unified edit modal w/ Existing Members: rs-groupmodal implementing
- [x] v1.1 chart-sharing spec + plan written (v1.1/spec.md, plan.md, research.md); decisions: no chart public links, member grants deferred, analyst=edit migration seed; M0 = public chart leak security fix — EXECUTION AWAITING GO
- [x] Group modal chips + unified edit: rs-groupmodal 6cead299 — review Spec ✅ / Approved (frames matched precisely; 4 Minor follow-ups: flatten-vs-existing dedupe redundant call, pending-member dup-guard gap, cancel-discards-removal test, drawer consolidation) — MERGED cb20f78b
- [x] People merged list: rs-people ca889bd5 + rs-people-be 4d25d092 (+4f23cbb6 select_related fix) — review Spec ✅ / Approved (gate judgment ACCEPTed: can_view_invitations keeps Members away from org-wide invite data) — MERGED webapp e32840bc / backend e3c3ded1; sandbox backend restarted
- [x] Share-flow alignment: rs-shareflow f1fe60d7 + 8a9ce774 (focus-ring fix) — review Spec ✅ / Approved after one Important a11y fix; transfer moved into grantee dropdown per frame (resolves D8: grantee-only reach on grants rtypes; metric/kpi combobox path kept) — MERGED b87ced14; merged suite 157/1796 green
- [x] Browser pass: People merged list MATCHES frame (icons, inviter attribution); group chips MATCH (teal internal/amber external, dup-guard); share modal flow structure live (title, requests header, wants-to rows, role tags, per-role general access) BUT content clips at dialog right edge at 1512px — overflow fixer dispatched (dialog width vs frames ~850px + row min-w-0 discipline)
- [x] Overflow fix b0aaf39f (sm:max-w-3xl + row min-w-0/truncate) — fixer live-verified at 1512/900/700px; independently confirmed in own session: modal matches frames, no clipping. ROUND 3 COMPLETE — webapp tip b0aaf39f, backend tip e3c3ded1, all local
- [x] Share modal exact-match: 4fced671 — review Spec ✅ / Approved (General access REMOVED from modal per user — D3 resolved design-wins; flat sections; staged-row pills; hairlines; 2 Minor dead-code follow-ups: orphaned setGeneralAccess, unused SHARING_GENERAL_ACCESS_UPDATED constant) + 44af4f6c frame nits (no tag on unknown-email staged rows, muted grant ✕) — browser-verified side-by-side against 'resource sharing New users.jpg'. NOTE: per-resource general access now bulk/API-only. Tips: webapp 44af4f6c, backend e3c3ded1
- [x] Group typeahead live bugs (user-reported): 20beabeb — dropdown stuck open after unknown-email staging (isFocused never cleared + Radix capture-phase Escape) fixed w/ onEscapeKeyDown→closeDropdownIfOpen layering; duplicate selection (Invite-row dup-guard hardcoded off, silent dedupe) fixed w/ Added/disabled + inline already-added hints; 7 regression tests; live-verified before/after — review running
- [ ] Share-modal Escape-layering parity fix dispatched (same gap, same pattern)
- [x] Share-modal Escape parity: 2e14bcc8 — first Escape closes dropdown only (staged rows survive), second closes dialog; 4 regression tests + live Playwright verification
- [ ] Intra-batch dup fixer running (reviewer findings on 20beabeb: same-email-twice-in-one-paste hint never fires + mixed-paste test + chip-retention assertion + plural notice wording)
- [x] Typeahead bug round CLOSED: 20beabeb + 2e14bcc8 + 8e2c0714 all re-reviewed — Spec ✅ / Approved (intra-batch dup fixed + verified empirically incl. stress case; mixed-paste tests; chip-retention assertion; bucketed plural notice; share-modal Escape parity clean). Remaining cosmetic note: pure intra-batch repeat says "already added" though no prior chip was visible. Tips: webapp 8e2c0714, backend e3c3ded1
- [x] v1.1 scope EXTENDED per user: KPI + Metric direct sharing "similarly" — spec §6b + plan Milestone 5 added (registry grants=True flip + modal/list mounting + bulk bars on kpi/metric lists; member grants deferred v1.1-wide; no public links; reverses the "Metric/KPI general-access-only" decision). M5 independent of chart milestones, can parallelize after M0. EXECUTION STILL AWAITING GO
