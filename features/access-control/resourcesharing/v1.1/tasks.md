# v1.1 Chart, KPI & Metric Sharing — Execution Ledger

Started 2026-07-16. Base: webapp `8e2c0714`, backend `e3c3ded1` (feature/resource-sharing, local only — NEVER push).
Pipeline: M0 → (M1 ∥ M5) → M2(+M4 backend) → (M3 ∥ M4 frontend) → full suites → Playwright end-to-end.

- [x] M0 — public chart leak fix: bb3ee83f, review Spec ✅ / Approved (6 sites fixed incl. 4 beyond research; reports immune; fails closed on malformed tabs; no status oracle). M2 follow-ups: promote _dashboard_chart_ids on consolidation, malformed-tabs regression test
- [x] M1 — charts join sharing model: rs-v11-charts c5ecc7aa — review Spec ✅ / Approved (4 adversarial probes; member exclusion holds incl. approve-path bypass; member_sharing registry flag). PROMOTED TO M2: raw-payload warehouse endpoints (charts_api.py:553,963,1096) Member-reachable resolver-blind; minors: _insert_grant comment cross-ref, null-role group edge, clean() comment
- [x] M5 — KPI/Metric grants + UI: rs-v11-km-be a440a2a3 + rs-v11-km-web 9e823d97 — review Spec ✅ / Approved (reviewer pre-simulated the M1 merge and mapped both conflicts); follow-ups: KPI drawer share affordance, non-admin invite copy vs 403 mismatch, Select-All vs status filter
- [x] MERGED: backend f9dc2800 (M1) + c6070ac8 (M5, additive member-block resolution per spec §6b — both mechanisms kept, documented in-code); webapp d19b64bd. Merged trees green (jest 160/1819; sharing core 418). Sandbox migrated 0179 + restarted
- [x] M2 — coverage engine + warnings backend: 55c7df36 — review Spec ✅ / Approved (adversarial: extend gated per-chart Edit, crafted extend_chart_ids revalidated, 6-endpoint warehouse ladder, ~6 fixed queries). M2.x DONE abd2603f — re-review Spec ✅ / Approved (empirical probe: copy stays locked at resolver level; parity guard trips on either-function drift; 570 regression green). RELEASE GATE: backend must NOT deploy without M3b (3 contract breaks documented). Product note: member-visible dashboards flag every tile — designer copy review (plan §8)
- [x] M3a — chart share UI (list action, bulk bar, detail affordance, member-block states; badge deferred — list payload lacks level fields): rs-v11-web a0015e5f+2435992d — review Spec ✅ / Approved after tsc fix loop — MERGED 5e4b3758
- [x] M3b — warning modals + M2 contract adaptations: rs-v11-m3b 5b643d59 — review Spec ✅ / Approved (reviewer re-ran 172 suites/1879 green, 0 new tsc errors; per-grant extend subsets verified against backend 400-check; view-only embed offers only REQUEST EDIT ACCESS; 409 fail-open can't mask real rejections). Minors → designer copy review (plan §8): "Extend all N" subset count, bulk single-item generic noun, frozen-report Member tile CSV 403 (deliberate, documented inline) — MERGED fd1a6e25, pushed to PR #347
- [x] M4 — chart 403 → request screen (generalized permanent-block rendering): in the M3a commit — MERGED 5e4b3758. Ticketed: dead bulk-delete selection code, __tests__/app/** unmatched by jest testMatch
- [x] final merges + full suites: webapp fd1a6e25 pushed to PR #347; backend abd2603f already on PR #1433. Sandbox backend restarted onto M2 code (deploy gate satisfied — paired webapp live via hot reload). Backend full suite 2789 passed / 31 skipped (warehouse-cred integration files excluded as always); webapp jest 171/172 suites green — sole failure AlertWizardModal is a pre-existing timeout flake under parallel load (passes in isolation 2s, untouched by this branch)
- [x] PR comment cleanup (user request 2026-07-17): backend ae5cae80 (82 files, −1041 lines, AST-verified comments-only; fixed 2 stale "charts not shareable" docstrings) + webapp 0ec6dbc1 (91 files, −790 lines, tsc baseline identical, 99 touched suites green). Both pushed; PR bodies refreshed (deploy gate now says both complete). Leftover: a few test describe-names still carry task markers (strings, deliberately untouched)
- [x] Playwright end-to-end pass (2026-07-17, sandbox on branch tip): chart share modal (member exclusion, dup guard, Escape layering, data.grant contract live) ✓; dashboard broadening confirm grant-add cancel/YES-extend (DB-verified: dashboard grant + per-chart grants) ✓; public toggle confirm (non-extendable copy, cancel reverts switch, YES mints token) ✓; embed warning covered-silent / gap-cancel-aborts-pick / gap-extend (level raise + grant + tile persisted through M2 validation) / view-only REQUEST-EDIT-ACCESS ✓; KPI/metric share + bulk (member attempt safely "1 skipped", general-access apply) ✓; member chart request-block inline message ✓; member tile CSV chart_id+dashboard_id → 200 ✓. Autosave-409 recovery jest-only (backend never emits without the race). BUGS FOUND+FIXED live: view→edit upgrade access requests 400'd "already have access" (5afd54b0 + wording-contract revert fb43f807, pushed). Follow-up ticket: bulk person search offers Members on deferred rtypes (backend skips safely — UI polish)
- [x] LEDGER CLOSED — v1.1 complete: backend fb43f807 / webapp 0ec6dbc1 on feature/resource-sharing, PRs #1433 + #347 (draft) current
